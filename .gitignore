node_modules/
dist/
build/
.DS_Store
*.log
.env
.env.local
data/worlds/
coverage/
*.tsbuildinfo

# Claude Code settings are machine-specific (scratchpad paths, local ports,
# one-off tool allowances), so they are not shared.
.claude/settings.json
.claude/settings.local.json
